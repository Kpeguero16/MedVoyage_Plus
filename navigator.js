const textElement = document.getElementById('display-text');
const newText = 'What is your sex?';

function changeText() {
    // Add any transition effects here if needed

    textElement.innerHTML = newText;
}